apiVersion: apps/v1
kind: Deployment
metadata:
  name: goilerplate
  namespace: apps          # ← FIXED: konsisten dengan Service & Ingress
  labels:
    app: goilerplate
    environment: development
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: goilerplate
  template:
    metadata:
      labels:
        app: goilerplate
        version: "${COMMIT_SHA}"
    spec:
      serviceAccountName: goilerplate
      imagePullSecrets:
        - name: ghcr-pull-secret
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 2000
      containers:
      - name: cloud-sql-proxy
        image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.14.1
        args:
          - "--port=5432"
          - "--private-ip"
          - "${CLOUD_SQL_INSTANCE}"
        securityContext:
          runAsNonRoot: true
          runAsUser: 65532
          allowPrivilegeEscalation: false
        resources:
          requests:
            cpu: 10m
            memory: 32Mi
          limits:
            cpu: 50m
            memory: 64Mi
        livenessProbe:
          tcpSocket:
            port: 5432
          initialDelaySeconds: 10
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
      - name: app
        image: ${IMAGE}
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http

        volumeMounts:
        - name: config-volume
          mountPath: /app/config
          readOnly: true

        envFrom:
        - secretRef:
            name: goilerplate-secret

        startupProbe:           # ← ADDED: cegah liveness kill pod saat init
          httpGet:
            path: /health
            port: 3000
          failureThreshold: 10
          periodSeconds: 5

        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3

        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

        resources:
          requests:
            cpu: 50m
            memory: 64Mi
          limits:
            cpu: 100m
            memory: 128Mi

      volumes:
      - name: config-volume
        configMap:
          name: goilerplate-config
          items:
          - key: config.yaml
            path: config.yaml
---
apiVersion: v1
kind: Service
metadata:
  name: goilerplate-service
  namespace: apps
  labels:
    app: goilerplate
    api-group: public
spec:
  selector:
    app: goilerplate
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    name: http
  type: ClusterIP
---
# NodePort for direct access (bypass Ingress) - Development only
# Access via: http://<NODE_IP>:30080
# Routes: /internal/*
apiVersion: v1
kind: Service
metadata:
  name: goilerplate-nodeport
  namespace: apps
  labels:
    app: goilerplate
    api-group: internal
spec:
  selector:
    app: goilerplate
  ports:
  - protocol: TCP
    port: 3000
    targetPort: 3000
    nodePort: 30080
    name: http
  type: NodePort
---
# Ingress for public APIs
# Routes: /api/*, /partner/*
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: goilerplate-ingress
  namespace: apps
  labels:
    app: goilerplate
  annotations:
    kubernetes.io/ingress.class: "gce"
    # Optional: force HTTPS redirect
    # networking.gke.io/managed-certificates: "goilerplate-cert"
    # kubernetes.io/ingress.allow-http: "false"
spec:
  rules:
  - host: goilerplate.sidejob.fun
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: goilerplate-service
            port:
              number: 80
      - path: /partner
        pathType: Prefix
        backend:
          service:
            name: goilerplate-service
            port:
              number: 80