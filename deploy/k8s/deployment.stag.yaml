apiVersion: apps/v1
kind: Deployment
metadata:
  name: goilerplate
  labels:
    app: goilerplate
    environment: staging
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: goilerplate
  template:
    metadata:
      labels:
        app: goilerplate
    spec:
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 2000
      containers:
      - name: app
        image: asia-southeast2-docker.pkg.dev/${PROJECT_ID}/goilerplate/goilerplate:${COMMIT_SHA}
        imagePullPolicy: IfNotPresent
        ports:
        - containerPort: 3000
          name: http
        
        volumeMounts:
        - name: config-volume
          mountPath: /app/config
          readOnly: true
        
        envFrom:
        - secretRef:
            name: goilerplate-secret
        
        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
        
        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3
          
        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 500m
            memory: 512Mi

      volumes:
      - name: config-volume
        configMap:
          name: goilerplate-config
          items:
          - key: config.yaml
            path: config.yaml
---
apiVersion: v1
kind: Service
metadata:
  name: goilerplate-service
  namespace: apps
  labels:
    app: goilerplate
    api-group: public
spec:
  selector:
    app: goilerplate
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    name: http
  type: ClusterIP
---
# Ingress for public APIs
# Routes: /api/*, /partner/*
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: goilerplate-ingress
  namespace: apps
  labels:
    app: goilerplate
  annotations:
    kubernetes.io/ingress.class: "gce"
    # Optional: force HTTPS redirect
    # networking.gke.io/managed-certificates: "goilerplate-cert"
spec:
  rules:
  - host: goilerplate.sidejob.fun
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: goilerplate-service
            port:
              number: 80
      - path: /partner
        pathType: Prefix
        backend:
          service:
            name: goilerplate-service
            port:
              number: 80