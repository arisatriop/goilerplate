apiVersion: apps/v1
kind: Deployment
metadata:
  name: goilerplate
  namespace: apps          # ← FIXED: konsisten dengan Service & Ingress
  labels:
    app: goilerplate
    environment: production
spec:
  replicas: 1
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: goilerplate
  template:
    metadata:
      labels:
        app: goilerplate
        version: "${COMMIT_SHA}"
    spec:
      serviceAccountName: goilerplate
      imagePullSecrets:
        - name: ghcr-pull-secret
      securityContext:
        runAsNonRoot: true
        runAsUser: 1000
        fsGroup: 2000
      containers:
      - name: cloud-sql-proxy
        image: gcr.io/cloud-sql-connectors/cloud-sql-proxy:2.14.1
        args:
          - "--port=5432"
          - "--private-ip"
          - "${CLOUD_SQL_INSTANCE}"
        securityContext:
          runAsNonRoot: true
          runAsUser: 65532
          allowPrivilegeEscalation: false
        resources:
          requests:
            cpu: 20m
            memory: 32Mi
          limits:
            cpu: 100m
            memory: 128Mi
        livenessProbe:
          tcpSocket:
            port: 5432
          initialDelaySeconds: 10
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3
      - name: app
        image: ${IMAGE}
        imagePullPolicy: Always
        ports:
        - containerPort: 3000
          name: http

        volumeMounts:
        - name: config-volume
          mountPath: /app/config
          readOnly: true

        envFrom:
        - secretRef:
            name: goilerplate-secret

        startupProbe:           # ← ADDED: cegah liveness kill pod saat init
          httpGet:
            path: /health
            port: 3000
          failureThreshold: 10
          periodSeconds: 5

        livenessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 30
          periodSeconds: 20
          timeoutSeconds: 5
          failureThreshold: 3

        readinessProbe:
          httpGet:
            path: /health
            port: 3000
          initialDelaySeconds: 10
          periodSeconds: 10
          timeoutSeconds: 5
          failureThreshold: 3

        resources:
          requests:
            cpu: 100m
            memory: 128Mi
          limits:
            cpu: 400m
            memory: 512Mi

      volumes:
      - name: config-volume
        configMap:
          name: goilerplate-config
          items:
          - key: config.yaml
            path: config.yaml
---
apiVersion: v1
kind: Service
metadata:
  name: goilerplate-service
  namespace: apps
  labels:
    app: goilerplate
    api-group: public
spec:
  selector:
    app: goilerplate
  ports:
  - protocol: TCP
    port: 80
    targetPort: 3000
    name: http
  type: ClusterIP
---
# Ingress for public APIs
# Routes: /api/*, /partner/*
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: goilerplate-ingress
  namespace: apps
  labels:
    app: goilerplate
  annotations:
    kubernetes.io/ingress.class: "gce"
    # Optional: force HTTPS redirect
    # networking.gke.io/managed-certificates: "goilerplate-cert"
    # kubernetes.io/ingress.allow-http: "false"
spec:
  rules:
  - host: goilerplate.sidejob.fun
    http:
      paths:
      - path: /api
        pathType: Prefix
        backend:
          service:
            name: goilerplate-service
            port:
              number: 80
      - path: /partner
        pathType: Prefix
        backend:
          service:
            name: goilerplate-service
            port:
              number: 80
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: goilerplate-hpa
  namespace: apps
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: goilerplate
  minReplicas: 1
  maxReplicas: 5
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: 70
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: 80