version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - gofmt
    - goimports
    - misspell
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck
