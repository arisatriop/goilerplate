version: "2"

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - ineffassign
    - misspell
    # T4.5 asks that no unused functions remain. Enforcing it here keeps that true instead of
    # being a sweep that decays. It only sees unexported symbols, so exported dead code still
    # needs a human to notice.
    - unused
  exclusions:
    rules:
      - path: _test\.go
        linters:
          - errcheck

formatters:
  enable:
    - gofmt
    - goimports
