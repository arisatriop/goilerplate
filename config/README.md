# Environment Variables Configuration

### Local development
You can only copy **config.example.yaml** to **config.yaml** and edit it. Don't use **.env** file for local development.

### Production
To configure env var in production, please follow the steps in **deploy/k8s/README.md**.

---